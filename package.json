{
  "name": "m1-ism-micmac-survey",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "start": "node server/m1-production-server.mjs"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
